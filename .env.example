# Root environment variables (if any shared variables are needed)
# Individual .env.example files exist in /contracts and /frontend directories

# Toggle private execution across tooling (Hardhat scripts, frontend API)
EXEC_PRIVATE=false

# Flashbots Protect relay configuration
FLASHBOTS_RELAY=https://relay.flashbots.net
# Optional: dedicated auth signer for Flashbots headers (keep secret)
FLASHBOTS_AUTH_KEY=
